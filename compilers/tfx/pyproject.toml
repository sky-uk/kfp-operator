[project]
name = "compiler"
version = "0.0.0" # set by setuptools-scm
description = ""
authors = []
requires-python = ">=3.9,<3.13"
dependencies = [
    "click>=8.1.8,<9",
    "PyYAML>=5.4.1",
    "setuptools~=70.0",
]

[project.optional-dependencies]
dev = [
    "pytest==8.4.2",
    "tfx==1.17.3; python_version < '3.10'",
    "tfx==1.21.0; python_version >= '3.10'",
    "shapely<2; python_version < '3.10'",
    "kfp>=2",
]

[project.scripts]
compiler = 'compiler.__main__:main'

[tool.setuptools]
packages = ["compiler"]

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "dirty-tag"

[build-system]
requires = ["setuptools>=61", "setuptools-scm"]
build-backend = "setuptools.build_meta"
