name: namespace/test
image: test-pipeline
framework:
  type: tfx
  parameters:
    components: pipeline.create_components
    beamArgs:
      - name: anArg
        value: aValue
env:
- name: foo
  value: bar
