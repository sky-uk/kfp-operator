natsConfig:
  subject: events
  serverConfig:
    host: localhost
    port: 4222
  jetstream:
    enabled: true
    streamName: run-completion-events
    subject: events
    maxAge: 1h          # Shorter retention during migration
    maxMsgs: 1000       # Limit message accumulation
    maxBytes: 10485760  # 10MB limit
serverConfig:
  host: localhost
  port: 50051
metricsConfig:
  host: localhost
  port: 8081
