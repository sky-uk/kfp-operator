apiVersion: pipelines.kubeflow.org/v1beta1
kind: Pipeline
metadata:
  name: quickstart-tfx-kfp2
spec:
  provider: provider-namespace/provider-name
  image: kfp-operator-quickstart-tfx-kfp2:v1
  framework:
    name: tfx
    parameters:
      pipeline: penguin_pipeline.pipeline.create_components
      beamArgs:
      - name: anArg
        value: aValue
