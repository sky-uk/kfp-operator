version: 1
context: mlops
triggering: master-and-prs

defaultNodes:
  cdBuild: xl-asdf
  prBuild: xl-asdf

modules:
  root:
    directory: /
    pipelineShape: buildOnly

defaultTaskExecution: 'make ${phase}'
