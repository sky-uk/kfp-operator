apiVersion: argoproj.io/v1alpha1
kind: ClusterWorkflowTemplate
metadata:
  name: {{ include "kfp-operator.fullname" . }}-experiment-steps
spec:
  templates:
  - name: create
    inputs:
      parameters:
      - name: provider-image
      artifacts:
      - name: experiment-definition
        path: /tmp/experiment-definition.yaml
        raw:
          data: |
            {{`{{workflow.parameters.experiment-definition}}`}}
      - name: provider-config
        path: /tmp/provider-config.yaml
        raw:
          data: |
            {{`{{workflow.parameters.provider-config}}`}}
    outputs:
      parameters:
      - name: experiment-id
        valueFrom:
          path: /tmp/id.txt
    metadata:
      {{- .Values.manager.argo.metadataDefaults | toYaml | nindent 6 }}
    container:
      image: "{{`{{inputs.parameters.provider-image}}`}}"
      command:
      - /provider
      args:
      - experiment
      - create
      - --experiment-definition
      - /tmp/experiment-definition.yaml
      - --provider-config
      - /tmp/provider-config.yaml
      - --out
      - /tmp/id.txt
    {{- include "kfp-operator.notEmptyYaml" .Values.manager.argo.containerDefaults | nindent 6 }}
  - name: update
    inputs:
      parameters:
      - name: provider-image
      artifacts:
      - name: experiment-definition
        path: /tmp/experiment-definition.yaml
        raw:
          data: |
            {{`{{workflow.parameters.experiment-definition}}`}}
      - name: provider-config
        path: /tmp/provider-config.yaml
        raw:
          data: |
            {{`{{workflow.parameters.provider-config}}`}}
    outputs:
      parameters:
      - name: experiment-id
        valueFrom:
          path: /tmp/id.txt
    metadata:
      {{- .Values.manager.argo.metadataDefaults | toYaml | nindent 6 }}
    container:
      image: "{{`{{inputs.parameters.provider-image}}`}}"
      command:
      - /provider
      args:
      - experiment
      - update
      - --experiment-definition
      - /tmp/experiment-definition.yaml
      - --provider-config
      - /tmp/provider-config.yaml
      - --experiment-id
      - '{{`{{workflow.parameters.experiment-id}}`}}'
      - --out
      - /tmp/id.txt
    {{- include "kfp-operator.notEmptyYaml" .Values.manager.argo.containerDefaults | nindent 6 }}
  - name: delete
    inputs:
      parameters:
      - name: provider-image
      artifacts:
      - name: provider-config
        path: /tmp/provider-config.yaml
        raw:
          data: |
            {{`{{workflow.parameters.provider-config}}`}}
    metadata:
      {{- .Values.manager.argo.metadataDefaults | toYaml | nindent 6 }}
    container:
      image: "{{`{{inputs.parameters.provider-image}}`}}"
      command:
      - /provider
      args:
      - experiment
      - delete
      - --experiment-definition
      - /tmp/experiment-definition.yaml
      - --provider-config
      - /tmp/provider-config.yaml
      - --experiment-id
      - '{{`{{workflow.parameters.experiment-id}}`}}'
    {{- include "kfp-operator.notEmptyYaml" .Values.manager.argo.containerDefaults | nindent 6 }}
---
apiVersion: argoproj.io/v1alpha1
kind: ClusterWorkflowTemplate
metadata:
  name: {{ include "kfp-operator.fullname" . }}-create-experiment
spec:
  arguments:
    parameters:
    - name: experiment-definition
    - name: provider-config
  entrypoint: main
  {{ if .Values.manager.argo.serviceAccount -}}
  serviceAccountName: {{ .Values.manager.argo.serviceAccount }}
  {{- end }}
  templates:
  - name: main
    outputs:
      parameters:
      - name: experiment-id
        valueFrom:
          parameter: '{{`{{steps.create.outputs.parameters.experiment-id}}`}}'
    steps:
    - - name: select-provider-image
        templateRef:
          name: {{ include "kfp-operator.fullname" . }}-common-steps
          template: select-provider-image
          clusterScope: true
    - - name: create
        arguments:
          parameters:
          - name: provider-image
            value: '{{`{{steps.select-provider-image.outputs.result}}`}}'
        templateRef:
          name: {{ include "kfp-operator.fullname" . }}-experiment-steps
          template: create
          clusterScope: true
---
apiVersion: argoproj.io/v1alpha1
kind: ClusterWorkflowTemplate
metadata:
  name: {{ include "kfp-operator.fullname" . }}-update-experiment
spec:
  arguments:
    parameters:
      - name: experiment-definition
      - name: provider-config
      - name: experiment-id
  entrypoint: main
  {{ if .Values.manager.argo.serviceAccount -}}
  serviceAccountName: {{ .Values.manager.argo.serviceAccount }}
  {{- end }}
  templates:
  - name: main
    outputs:
      parameters:
      - name: experiment-id
        valueFrom:
          parameter: '{{`{{steps.update.outputs.parameters.experiment-id}}`}}'
    steps:
    - - name: select-provider-image
        templateRef:
          name: {{ include "kfp-operator.fullname" . }}-common-steps
          template: select-provider-image
          clusterScope: true
    - - name: update
        arguments:
          parameters:
          - name: provider-image
            value: '{{`{{steps.select-provider-image.outputs.result}}`}}'
        templateRef:
          name: {{ include "kfp-operator.fullname" . }}-experiment-steps
          template: update
          clusterScope: true
---
apiVersion: argoproj.io/v1alpha1
kind: ClusterWorkflowTemplate
metadata:
  name: {{ include "kfp-operator.fullname" . }}-delete-experiment
spec:
  arguments:
    parameters:
    - name: provider-config
    - name: experiment-id
  entrypoint: main
  {{ if .Values.manager.argo.serviceAccount -}}
  serviceAccountName: {{ .Values.manager.argo.serviceAccount }}
  {{- end }}
  templates:
  - name: main
    steps:
    - - name: select-provider-image
        templateRef:
          name: {{ include "kfp-operator.fullname" . }}-common-steps
          template: select-provider-image
          clusterScope: true
    - - name: delete
        arguments:
          parameters:
          - name: provider-image
            value: '{{`{{steps.select-provider-image.outputs.result}}`}}'
        templateRef:
          name: {{ include "kfp-operator.fullname" . }}-experiment-steps
          template: delete
          clusterScope: true
