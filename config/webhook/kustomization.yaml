resources:
- manifests.yaml
- service.yaml

namespace: kfp-operator-system

# [CERTMANAGER] CA injection is handled by the 'replacements' in
# config/default/kustomization.yaml, which populate the cert-manager.io/inject-ca-from
# annotation on the webhook configuration.

configurations:
- kustomizeconfig.yaml
