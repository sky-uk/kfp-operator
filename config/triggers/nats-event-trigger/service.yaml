apiVersion: v1
kind: Service
metadata:
  name: nats-event-trigger
  namespace: kfp-operator-system
spec:
  selector:
    app: nats-event-trigger
  ports:
  - protocol: TCP
    port: 50051
    targetPort: grpc-server
