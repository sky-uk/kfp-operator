resources:
- manager.yaml
- providers.yaml
- workflows/common.yaml
- workflows/pipelines.yaml
- workflows/provider.yaml
- workflows/rbac.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: manager-config
  namespace: kfp-operator-system
  files:
  - controller_manager_config.yaml
