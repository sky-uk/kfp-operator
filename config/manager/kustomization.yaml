resources:
- manager.yaml
- workflows/common.yaml
- workflows/pipelines.yaml
- workflows/experiments.yaml
- workflows/runconfigurations.yaml
- providers.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: manager-config
  namespace: kfp-operator-system
  files:
  - controller_manager_config.yaml
